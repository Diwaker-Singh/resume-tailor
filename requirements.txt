# Runtime dependencies are declared in pyproject.toml.
# For development install everything (incl. pytest) with:
#     pip install -e ".[dev]"
# This file is kept for `pip install -r requirements.txt` convenience:
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0
